/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

// const nextConfig = {
//     i18n: {
//         locales: ['en', 'th'], // รายการภาษาที่รองรับ
//         defaultLocale: 'en',   // ภาษาที่ใช้เป็นค่าเริ่มต้น
//       },
//       reactStrictMode: true,    // การตั้งค่าให้ React ทำงานในโหมด Strict
//       swcMinify: true,          // ใช้ SWC แทน Terser สำหรับการลดขนาด JavaScript
//       images: {
//         domains: ['example.com'], // รายการโดเมนที่อนุญาตให้โหลดภาพจาก
//       },
// };

// export default nextConfig;
